{
    "private": true,
    "scripts": {
        "dev": "vite",
        "build": "composer self-update && composer update && composer install --no-dev --prefer-dist && cp resources/css/* public/css/ && mix --production && php artisan route:cache && php artisan view:cache && npm install && npm run prod",
        "mix": "mix"
    },
    "devDependencies": {
        "axios": "^1.1.2",
        "laravel-mix": "^6.0.49",
        "laravel-vite-plugin": "^0.7.2",
        "resolve-url-loader": "^5.0.0",
        "sass": "^1.58.1",
        "sass-loader": "^12.1.0",
        "vite": "^4.0.0"
    },
    "dependencies": {
        "boostrap": "^2.0.0",
        "bootstrap": "^5.2.3",
        "jquery": "^3.6.3"
    }
}
